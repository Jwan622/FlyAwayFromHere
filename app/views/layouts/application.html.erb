<!DOCTYPE html>
<html>
<head>
  <title>FlyAwayFromHere</title>
  <link href='http://fonts.googleapis.com/css?family=Titillium+Web:400,300italic&subset=latin,latin-ext' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="main-body container">
    <%= render "layouts/nav_bar" %>
    <% if itinerary.present? %>
      <div class="complete-itinerary">
        Finish Planning and
          <%= link_to "Fly Away.", itinerary_path, class: "itinerary-underline" %>
      </div>
    <% end %>
    <% flash.each do |name, msg| %>
      <%= content_tag :div, msg, :class => "flash-#{name}" %>
    <% end %>
    <%= yield %>
  </div>
  <%= render "layouts/footer_bar" %>
</body>
</html>
